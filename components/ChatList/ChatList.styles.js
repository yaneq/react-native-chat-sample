import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  noteContainer: {
    padding: 25,
    marginHorizontal: 10,
    marginVertical: 10,
    backgroundColor: '#ddd',
    borderRadius: 10,
    fontSize: 40,
  },
});
